<% scene = DATA.scene %>

<h1><%= h scene['title'] %></h1>
<p class="scene-description"><%= h scene['description'] %></p>

<h2>Map</h2>
<%= notes_map_stub(
      entries: DATA.note_maps,
      dm_view: dm?,
      active_only: true,
      interactive: true,
      scene_state: SCENE_STATE
    ) %>

<h2>Journal</h2>
<%= notes_journal_stub(
      entries: DATA.notes,
      dm_view: dm?,
      active_only: true
    ) %>

<h2>Characters</h2>
<%= notes_characters_stub(
      entries: DATA.characters_of_interest,
      dm_view: dm?,
      active_only: true
    ) %>

<h2>Images</h2>
<%= notes_images_stub(
      entries: DATA.note_images,
      dm_view: dm?,
      active_only: true
    ) %>

<% if scene['show_initiative'] %>
  <h2>Initiative</h2>
  <%= initiative_stub(
        turns: DATA.initiative_turns,
        current_combat_id: session[:current_turn] || DATA.combat_state['current_turn'],
        dm_view: dm?
      ) %>
<% end %>
