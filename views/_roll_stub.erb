<%#
  Roll Resolution Stub.

  Locals:
    roll       — required, the Roll data hash.
    rows_only  — optional boolean. When true, emit only the roll group
                 <tbody> (so a parent stub can place it inside its own
                 table). When false/absent, emit the full Rolls wrapper.
%>
<%
  require 'json'

  rows_only = defined?(rows_only) ? rows_only : false

  starting_text =
    if roll[:starting_value].to_i.positive?
      ", R+#{roll[:starting_value]}"
    elsif roll[:starting_value].to_i.negative?
      ", R-#{roll[:starting_value].abs}"
    else
      ''
    end

  fmt_mod = lambda do |mod|
    next nil if mod.nil?
    sign = (mod[:sign] || (mod[:amount].to_i.negative? ? :neg : :pos))
    sign_ch = sign == :neg ? '-' : '+'
    magnitude = mod[:max] ? '*' : mod[:amount].abs.to_s
    "#{sign_ch}#{magnitude}"
  end

  serialize_mod = lambda do |mod|
    next nil if mod.nil?
    sign = (mod[:sign] || (mod[:amount].to_i.negative? ? :neg : :pos))
    { sign: sign.to_s, count: mod[:amount].to_i.abs, max: mod[:max] ? true : false }
  end

  reroll_text = fmt_mod.call(roll[:reroll])
  nudge_text  = fmt_mod.call(roll[:nudge])
  has_reroll  = !reroll_text.nil?
  has_nudge   = !nudge_text.nil?
  row_count   = 1 + (has_reroll ? 1 : 0) + (has_nudge ? 1 : 0)

  group_config = {
    dice_count: roll[:dice_count],
    tn:         roll[:tn],
    die_size:   roll[:die_size],
    reroll:     serialize_mod.call(roll[:reroll]),
    nudge:      serialize_mod.call(roll[:nudge])
  }
%>

<% unless rows_only %>
<div class="rolls-wrapper" data-stub="roll">
  <div class="rolls-header">
    <span class="rolls-title">Rolls</span>
    <div class="rolls-actions">
      <button type="button" class="btn-roll-all">Roll All</button>
      <button type="button" class="btn-confirm-all">Confirm All</button>
    </div>
  </div>
  <table class="stub-table roll-table">
    <colgroup>
      <col class="col-character">
      <col class="col-mod">
      <col class="col-mod">
      <col class="col-dice">
      <col class="col-result">
      <col class="col-result">
      <col class="col-lock">
    </colgroup>
    <thead>
      <tr>
        <th>Character</th>
        <th>Reroll</th>
        <th>Nudge</th>
        <th>Dice</th>
        <th>Result</th>
        <th>Crits</th>
        <th></th>
      </tr>
    </thead>
<% end %>

<tbody class="roll-group" data-config='<%= group_config.to_json %>'>
  <tr class="roll-row row-initial">
    <td rowspan="<%= row_count %>" class="character-cell">
      <span class="stub-line creature-name"><%= roll[:creature_name] %></span>
      <span class="stub-line params"><%= roll[:dice_count] %> dice @ TN <%= roll[:tn] %><%= starting_text %></span>
      <span class="stub-line roll-name"><em>(<%= roll[:roll_name] %>)</em></span>
    </td>
    <td class="mod-cell"></td>
    <td class="mod-cell"></td>
    <td class="dice-cell">
      <span class="dice-placeholder">[ &mdash; ]</span>
    </td>
    <td rowspan="<%= row_count %>" class="result-cell">
      <input class="result-input" type="text" value="<%= roll[:dois] %>">
    </td>
    <td rowspan="<%= row_count %>" class="result-cell">
      <input class="result-input" type="text" value="<%= roll[:critical_count] %>">
    </td>
    <td rowspan="<%= row_count %>" class="lock-cell">
      <button type="button" class="lock-btn" aria-label="Toggle lock"></button>
    </td>
  </tr>

  <% if has_reroll %>
    <tr class="modifier-row row-reroll">
      <td class="mod-cell">
        <span class="mod-badge mod-reroll" data-tooltip="<%= roll[:reroll][:label] %>"><%= reroll_text %></span>
      </td>
      <td class="mod-cell"></td>
      <td class="dice-cell"></td>
    </tr>
  <% end %>

  <% if has_nudge %>
    <tr class="modifier-row row-nudge">
      <td class="mod-cell"></td>
      <td class="mod-cell">
        <span class="mod-badge mod-nudge" data-tooltip="<%= roll[:nudge][:label] %>"><%= nudge_text %></span>
      </td>
      <td class="dice-cell"></td>
    </tr>
  <% end %>
</tbody>

<% unless rows_only %>
  </table>
</div>
<% end %>
